export class Broker_class{
  id: number;
  name: string;
  gold: number;
  constructor(id:number, name: string, gold: number) {
    this.id = id;
    this.name = name;
    this.gold = gold;
  }
}
